# 本地地址池重叠限制说明

## 约束描述

同一VPN内不同地址池的地址段不能重叠；同一地址池内各地址段也不能重叠。未配置VPN的地址池视为同一VPN。

## 适用场景

地址池新增、扩容、迁移、地址池合并或多DNN共享地址资源场景。

## 影响

若地址段重叠，可能导致地址分配冲突、回程路由异常或新增配置失败。

## 建议

新增地址段前先建立全局地址台账，并在导入或下发前做地址冲突校验。

## 相关命令

ADD POOL、ADD SECTION、MOD DNN、MOD APN
