# 切片到DNN映射规划约束说明

## 约束描述

S-NSSAI与DNN的映射应同时满足签约、接入控制、UPF选择组和路由出口规划，不建议仅在SMF单侧孤立配置。

## 适用场景

多切片、多业务DNN、园区专网或边缘业务场景。

## 影响

若规划不一致，可能出现终端接入成功但会话落点错误、业务无法访问或切片隔离失效。

## 建议

为每个高价值切片建立标准映射清单，并与签约和UPF拓扑保持一致。

## 相关命令

ADD SLICEDNNMAP、ADD DNN、MOD DNN
